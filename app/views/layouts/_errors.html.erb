<% if object.errors.any? %>
  <h3>Seriously, just follow instructions and fill in the forms, already:</h3>
  <ul>
    <% object.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
<% end %>